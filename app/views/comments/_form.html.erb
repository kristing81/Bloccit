<%= simple_form_for [post, comment], html: { class: 'form-vertical' }  do |f| %>
  <%= f.input :body, required: false, placeholder: "Add comment" %>
  <%= f.button :submit, class: 'btn btn-success' %>
<% end %> 